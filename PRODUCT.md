# Product

## Register

product

## Users

开发者和 Agent 工作流设计者在本机运行、调试与回顾 Agent 会话时使用。Codex 是当前首个接入的数据源，产品后续可扩展支持其他本地 Agent。

## Product Purpose

ThreadScope 将分散的会话、用户请求、Agent 响应、工具调用、Skill、MCP 与 Subagent 活动组织成可快速浏览和深入检查的本地界面。成功意味着用户能迅速定位目标会话，理解一次交互的完整执行路径，并在需要时查看精确的底层事件与原始数据。

## Brand Personality

克制、精确、高密度。界面应像可靠的开发工具：信息丰富但不喧闹，熟悉而高效，并对运行状态保持诚实。

## Anti-references

避免装饰性仪表盘、卡片堆叠、过多状态文字、无意义的形状与颜色、夸张动效，以及为了展示技术而增加的交互复杂度。不要把 Codex 专有概念固化成无法扩展到其他 Agent 的产品结构。

## Design Principles

1. 会话优先：用户请求与 Agent 响应是主线，工具和编排活动作为可追溯的辅助层。
2. 渐进披露：列表保持紧凑，完整内容、元数据和原始数据按需展开。
3. 语义胜过装饰：颜色、图形和状态只在表达真实差异时使用。
4. 熟悉且可预测：遵循成熟开发工具和 Vercel 界面的交互惯例。
5. 面向多 Agent：数据模型、命名与布局不依赖单一 Agent 实现。

## Accessibility & Inclusion

以 WCAG AA 为基线，支持键盘操作、可见焦点、足够的文字与状态对比度，并尊重减少动态效果偏好。状态不能只依赖颜色表达。
